# Locales

Each language has its own folder:

```text
locales/<language-code>/gamedata.json
```

`locales/en/gamedata.json` is the English baseline. Other locale files must keep the same translatable string paths.

Only edit the text values. Do not rename keys or change the JSON structure.
